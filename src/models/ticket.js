export default class Ticket {
    constructor(user, agent, title, description) {
      this.user = user;
      this.agent = agent;
      this.title = title;
      this.description = description;
    }
  }