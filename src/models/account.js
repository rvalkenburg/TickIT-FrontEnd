export default class Account {
    constructor(username, password, firstname, surname, company) {
      this.username = username;
      this.password = password;
      this.firstname = firstname;
      this.surname = surname;
      this.company = company;
    }
  }